# The reports app is intentionally model-less: it reads and aggregates data
# owned by other apps (animals, breeding, health, feed, sales, finance)
# rather than duplicating storage. See apps/reports/services.py.
